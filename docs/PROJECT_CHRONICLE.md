# 《空谷项目志》

> Konggu / 空谷项目历史、版本沿革与决策档案  
> 建档日期：2026-07-23  
> 当前记载范围：2026-06-02 至 2026-08-10
> 当前主线：`kongguV0.1`  
> 当前源码版本标识：`0.4.1`（质量基线已提交并验证，未建立标签、未发布）

---

## 序言

空谷不是从一开始就定型的产品。

它先以 V0.1 的桌面程序起步，随后尝试过本地 Web 形态，也曾为了收缩维护面而彻底移除桌面端。经过对实际使用场景的重新确认，项目最终回到“完全离线的 Windows 桌面工具”这条主线，并逐步把重点从“能够识别课表”推进到“能够给出可信、可复核、可用于真实排班的结果”。

空谷属于青禾计划系列项目。它面向中英合办及中外合作办学场景，解决多人中方、英方课表收集与合并中大量重复、耗时、低价值的人工劳动。

项目最核心的业务判断始终是：

> 同一成员只有在中方课表和英方课表中都没有课程占用，才算真正空闲。

本志不只是功能清单。它记录项目为什么改变、每个阶段解决了什么问题、哪些道路被尝试后放弃，以及空谷如何从一个可运行的工具逐步演变为强调离线、可信、可审计的业务系统。

---

## 一、档案说明

### 1.1 记载依据

本志主要依据以下材料整理：

1. Git 提交历史、标签、分支和代码变更。
2. 当前仓库中的 `README.md`、架构、测试、识别规则和审计基线。
3. 已完成的构建、测试、真实样本审计与阶段性工作记录。
4. 2026-08-08 补录时对当前仓库、版本标识与验证记录的核验。

### 1.2 状态标记

| 标记 | 含义 |
| --- | --- |
| 已提交 | 已进入 Git 提交历史，可通过提交号追溯 |
| 已验证 | 有测试、构建、审计或产物作为验证依据 |
| 在研 | 当前工作区已有实现，但尚未形成正式提交 |
| 历史形态 | 曾经存在，后来被替换或删除 |
| 待确认 | 现有材料不足以得出确定结论 |

### 1.3 史料边界

仓库现有 Git 历史从 2026-06-02 开始，因此本志不推测此前是否存在未纳入版本管理的原型或构思。若后续找到更早的设计稿、聊天记录、安装包或源代码，可在“前史”章节补录。

---

## 二、项目总览

### 2.1 项目身份

| 项目 | 记录 |
| --- | --- |
| 中文名 | 空谷 |
| 英文名 | Konggu |
| 所属计划 | 青禾计划 |
| 主要场景 | 中英合办、中外合作办学中的多人课表汇总与排班 |
| 核心输入 | 多名成员的中方、英方课表 PDF |
| 核心处理 | 文本层提取、离线 OCR、版式识别、课程结构化、中英占用合并 |
| 核心输出 | 可复核的集体空课结果与 Excel |
| 当前运行形态 | Windows 离线桌面应用 |
| 当前技术主干 | Tauri + Vite/TypeScript + Python sidecar |
| 数据原则 | 本地处理、异常显性、拒绝无依据猜测 |

### 2.2 项目使命的形成

早期的空谷更接近“多人课表 PDF 解析与空课表生成工具”。随着真实业务场景被不断澄清，项目定位逐步收紧为：

> 为中英合办学生组织提供离线的双课表合并能力，把负责人从收集、查看、比对和统计课表的重复劳动中释放出来，同时让缺失、冲突和识别失败能够被看见、被复核。

这一定位改变了项目的评价标准：功能数量不再是首要目标，结果是否可信、错误是否可见、真实样本是否能稳定处理，成为后续迭代的核心。

---

## 三、编年大事记

| 日期 | 阶段 | 大事 | 状态与证据 |
| --- | --- | --- | --- |
| 2026-06-02 | V0.1 起点 | 首个仓库版本形成，包含课表核心、桌面入口、配置、测试与打包骨架 | 已提交：`dbdc8c4` |
| 2026-06-04 | 架构奠基 | 引入应用启动层、服务层、拆分 GUI，并适配核心模块 | 已提交：`6df981f` 至 `9bd7b81` |
| 2026-06-04 | Web 探索 | 新增本地 FastAPI Web 界面和任务管理能力 | 已提交：`390e23d` |
| 2026-06-04 | 第一轮工程化 | 增加分层工作流测试，移除旧式辅助代码 | 已提交：`32f1d53`、`bf899d4` |
| 2026-06-20 | 仓库治理 | 清理重复素材、占位包和旧分支，主分支统一为 `kongguV0.1` | 已提交、已验证 |
| 2026-06-20 | Web-only 阶段 | 完整移除当时的 CustomTkinter 桌面客户端，项目暂时收缩为本地 Web 形态 | 历史形态；`69021fa` |
| 2026-07-07 | 桌面主线重建 | 放弃 Web-only 作为最终产品形态，转为 Tauri 桌面壳 + Python sidecar 的完全离线 Windows 应用 | 已提交：`7985d61` |
| 2026-07-07 | 产品定位定型 | 明确聚焦中英合办双课表合并和减少重复人工劳动，强化输入契约、异常提示与 README | 已提交、已验证 |
| 2026-07-07 | 桌面体验完善 | 增加流程指引、状态反馈、文件校验、成员检查和友好错误提示 | 已验证 |
| 2026-07-07 | 离线发布链打通 | 冻结 OCR 资源，完成 sidecar、前端、Tauri、NSIS 全链路构建 | 已验证 |
| 2026-07-08 | 0.3.0 发布 | 仓库版本升级为 `0.3.0`，形成 `v0.3.0` 标签 | 已提交：`9e1b3e5` |
| 2026-07-08 | 发布后清理 | 删除无用生成物和不再需要的资源，进一步收紧仓库 | 已提交：`382c0e1` |
| 2026-07-09 至 07-10 | 识别强化 | 围绕真实 PDF 样本迭代版式识别、NFKC 归一化、视觉回退和 OCR 选择逻辑 | 已验证 |
| 2026-07-09 至 07-10 | 输入预检强化 | 拒绝占位姓名、同内容异姓名和不明确课表类型，避免坏数据静默进入结果 | 已验证 |
| 2026-07-23 | 识别审计基线 | 固化 76 份参考 PDF 的解析审计基线、专用版式规则、审计工具和回归测试 | 已提交：`d860f7c` |
| 2026-07-23 | 质量治理落地 | 持久任务、SQLite 状态、人工复核、修正复用、解析签名和审计事件工作流完成首轮提交 | 已提交：`1fc6e4e` |
| 2026-08-08 | 治理层建立 | 落地 `AGENTS.md`、MEMORY 四文件、参考资料库与配套文档，统一空谷命名 | 已提交：`b7b4977` |
| 2026-08-08 | 0.3.1 文档升级 | README 全中文重写，版本标识升至 0.3.1 | 已提交：`d039a6e` |
| 2026-08-08 | 0.3.2 文档升级 | README v0.4 结构优化，版本标识升至 0.3.2 | 已提交：`90f5b65` |
| 2026-08-09 | 0.4.0 PDF 结构诊断 | 研究并接入 Firecrawl PDF Inspector 旁路诊断，逐页展示 OCR 建议、编码和复杂版式信号 | 已提交并验证：`e7ab4ef` |
| 2026-08-10 | 0.4.1 质量与隐私基线 | 建立脱敏样本索引、人工真值模板、质量门禁回归和发布记录模板，清理仓库内敏感标识 | 已提交并验证：`52c2c6a` |

---

## 四、第一纪：V0.1 与原始桌面程序

### 4.1 起点

2026-06-02，提交 `dbdc8c4` 形成了当前可追溯历史中的第一个版本。

这个版本已经具备空谷的原始骨架：

- 课表解析核心 `core/schedule_core.py`；
- 结构化模型与旧格式适配；
- 基础配置与参考库；
- 基于桌面界面的主程序；
- PyInstaller 打包文件；
- 基础测试；
- OCR 模型准备工具。

这一时期的主要目标是把“PDF 课表转换为可用空课结果”跑通。项目规模已经不小，但大量业务、界面和流程仍集中在单一桌面入口中，可维护性和复用能力有限。

### 4.2 6 月 4 日的分层重构

2026-06-04，项目连续完成了一组架构提交：

| 提交 | 作用 |
| --- | --- |
| `f5d6e25` | 更新 V0.1 项目配置 |
| `6df981f` | 增加应用启动层 |
| `8a7b912` | 建立课表工作流服务层 |
| `3132679` | 拆分桌面启动器和 GUI 层 |
| `390e23d` | 增加本地 FastAPI Web 界面 |
| `9bd7b81` | 让核心课表逻辑适配分层架构 |
| `32f1d53` | 增加 V0.1 分层工作流测试 |
| `bf899d4` | 移除过时辅助代码 |

这一轮变化的历史意义不在于多了一个 Web 页面，而在于空谷第一次把“界面”“业务编排”“核心解析”拆开。此后桌面端、Web 端或其他入口都可以围绕同一个业务核心演进。

当时的测试记录曾达到 `47 passed, 1 warning`。这是早期架构重构完成后的历史验证值，不能与后续测试数量直接比较。

---

## 五、第二纪：本地 Web 探索与 Web-only 收缩

### 5.1 为什么尝试 Web

本地 Web 界面提供了比传统桌面 GUI 更灵活的布局、交互和任务展示方式。FastAPI 路由、任务管理器、静态资源和结果页面在这一阶段建立起来，证明核心业务可以脱离原始桌面窗口运行。

这次探索带来了两个长期收益：

1. 界面不再与解析核心强绑定；
2. 任务状态、文件导入、处理和结果展示开始具备清晰边界。

### 5.2 2026-06-20：主动做减法

6 月 20 日，项目进行了集中治理：

- 更新 Web 端素材；
- 删除重复图片和无实际代码的占位包；
- 清理旧分支；
- 将默认分支统一为 `kongguV0.1`；
- 删除当时的 CustomTkinter 桌面端和桌面打包依赖；
- 将 README 改为符合当时 Web-only 状态的双语说明。

提交 `69021fa` 共涉及 28 个文件，约为 154 行新增、2168 行删除。它代表一次明确的收缩：当时项目选择保留本地 Web 入口，彻底放弃旧桌面客户端，而不是长期维护两套界面。

桌面测试随桌面代码一起移除后，历史测试记录为 `45 passed, 1 warning`。

### 5.3 这条路线为什么没有成为终局

Web-only 是一次有价值的架构试验，但它没有完全满足最终产品需求：

- 普通使用者更需要直接安装和打开的 Windows 软件；
- 本地服务的启动、端口和浏览器环境增加了使用门槛；
- OCR 模型、Python 运行时和配置需要统一封装；
- 项目强调离线和个人课表隐私，桌面安装包更符合交付预期。

因此，7 月的桌面化不是简单地恢复旧 GUI，而是在分层架构基础上选择了新的桌面技术路线。

---

## 六、第三纪：离线 Tauri 桌面端重建

### 6.1 2026-07-07：技术路线转折

提交 `7985d61` 将项目转换为离线 Tauri 桌面应用。这是空谷历史上最大的一次产品形态转折。

新结构为：

```text
Tauri Windows 桌面壳
  -> Vite / TypeScript 前端
  -> Python sidecar 命令桥
  -> app/services 工作流
  -> core/schedule_core 解析核心
  -> 本地 OCR、缓存、配置和 Excel
```

旧的 FastAPI Web 路由、模板和静态页面被移除；Python 解析能力没有被重写，而是通过 sidecar 继续复用。这样既保留了 Python 在 PDF、OCR 和 Excel 处理上的优势，又获得了现代桌面界面的安装、交互和资源封装能力。

### 6.2 完全离线成为产品底线

这一时期建立了完整的离线资源机制：

- 使用 PP-OCRv4 mobile 检测与识别模型；
- 通过 `config/ocr_models.json` 管理资源来源、大小和校验值；
- 生成 `resources/offline_manifest.json`；
- 将资源随安装包封装；
- 在用户目录中修复和维护可写资源；
- 默认禁止运行时在线下载 OCR 模型。

离线不只是技术特点，它回应了真实业务中的两个问题：课表包含个人信息，且软件不应依赖现场网络才能完成排班工作。

### 6.3 桌面工作流与交互完善

前端逐步形成明确的使用链：

```text
检查离线材料
  -> 选择 PDF
  -> 校验文件名与课表类型
  -> 解析中方、英方课表
  -> 查看空课预览
  -> 处理成员异常
  -> 导出 Excel
```

界面增加了五步流程条、状态标签、文件分组、缺失配对提示、成员检查、日志清理和友好错误显示。浏览器预览或非 Tauri 环境不再暴露底层 `TypeError`，而是提示用户需要在 Konggu 桌面端运行相关功能。

### 6.4 输入契约开始严格化

早期系统会在无法识别课表类型时做默认推断。随着产品目标从“尽量处理”转为“输出可信”，这一行为被取消。

正式形成的输入规则包括：

1. 文件必须是 PDF；
2. 文件名应包含真实成员姓名；
3. 文件名必须明确标出“中方”或“英方”；
4. 同一成员应同时提交两类课表；
5. 无法判断时拒绝解析，不静默猜测。

这意味着空谷开始把“拒绝不可靠输入”视为产品能力，而不是失败。

---

## 七、第四纪：0.3.0 与产品形态定型

### 7.1 产品叙事定型

2026-07-07 至 07-08，空谷完成了从技术工具到具体业务产品的定位收束：

- 不再把自己描述成泛用课表工具；
- 明确服务中英合办、中外合作办学场景；
- 核心价值是减少重复、耗时、低价值的人工核对；
- 核心规则是中方和英方都无课才算真正空闲；
- 结果必须附带缺失、冲突、解析失败等风险信息。

README 也在这一时期被改写为完整的项目主页，解释了用户、场景、流程、输入契约、输出含义、架构、离线资源、测试和路线边界。

### 7.2 版本 0.3.0

提交 `9e1b3e5` 将 `package.json`、Tauri 配置等版本标识更新为 `0.3.0`，仓库建立 `v0.3.0` 标签。

该提交还整合了：

- OCR 处理增强；
- 桌面工作流和输入识别完善；
- 新应用图标；
- UI 流程与反馈优化；
- 异常测试补充；
- 离线资源和 sidecar 打包改进。

紧随其后的 `382c0e1` 删除了无用生成资源和 Tauri 自动生成 schema，延续了项目“不保留无实际价值内容”的治理习惯。

### 7.3 安装包构建史实

7 月 7 日，项目曾完整执行以下发布链：

```text
PyInstaller 构建 Python sidecar
  -> Vite 构建前端
  -> Tauri 编译桌面程序
  -> NSIS 生成 Windows 安装包
```

当次已验证的安装包文件名为 `Konggu_0.1.0_x64-setup.exe`，大小约 248 MB。随后仓库版本标识升级为 `0.3.0`。

因此，历史上可以确认“完整安装链已打通”，但不应把旧文件名的安装包直接记作已验证的 0.3.0 安装包。若要形成正式发布档案，应重新构建并记录 `0.3.0` 安装包的哈希、大小和验证结果。

---

## 八、第五纪：真实样本、识别审计与可信结果

### 8.1 从“能跑”到“可信”

进入 7 月中旬后，项目的主要矛盾不再是界面或安装，而是不同来源的真实课表 PDF 能否被稳定识别。

这一阶段确立了长期原则：

> 错误的 Excel 只是症状，真正要修的是解析器、识别规则、输入预检和回归体系，而不是手工修改某一个输出文件。

### 8.2 文本层优先，OCR 作为回退

空谷的识别链并非对所有 PDF 直接做 OCR：

```text
PDF 文本层检查
  -> 文本质量与版式判断
  -> 可用时直接解析文本和坐标
  -> 不足时调用离线 OCR
  -> 必要时使用视觉版式回退
  -> 比较完整度和可信度
  -> 选择更可靠的结构化结果
```

这一路线降低了不必要的 OCR 成本，也保留了原生 PDF 文本层的精确信息。

### 8.3 专用版式与 Unicode 兼容

真实样本暴露出兼容表意字符问题：某些 PDF 中视觉上正常的汉字，底层编码并不是常用 Unicode 形式，导致版式特征匹配失败。

项目在版式检测前增加 NFKC 归一化，并在识别规则变化时提升解析缓存版本，避免旧缓存掩盖新规则效果。

随后形成了多类专用版式识别，包括：

- 中英合办英文 Web 课表；
- 本科全学期中文课表；
- 旧版中文 Web 课表；
- 英文周网格课表。

### 8.4 输入预检成为第一道质量门

真实样本库中还发现了两类不能交给解析器“自行处理”的坏来源：

- 使用“路人甲”等测试或占位姓名的文件；
- PDF 内容完全相同，但文件名对应不同成员的文件。

项目把这些检查前移到 PDF 导入层和审计工具中。坏来源不再被误记为自动通过，而是进入明确的待复核状态。

### 8.5 2026-07-23 解析基线

提交 `d860f7c` 固化了第一份正式的解析审计基线：

| 指标 | 基线值 |
| --- | ---: |
| 参考 PDF 总数 | 76 |
| 自动通过 | 73 |
| 占位姓名待复核 | 1 |
| 同内容异姓名待复核 | 2 |
| 回归测试数 | 83 |
| 自动通过率 | 96.1% |

自动通过率按 `73 / 76` 计算。剩余 3 份没有被“强行识别成功”，而是被正确挡在预检和复核环节。这体现了空谷对可信度的取舍：宁可明确说“需要确认”，也不把可疑输入包装成成功结果。

该提交还新增或强化了：

- `config/timetable_layout_profiles.json`；
- 专用课表识别说明；
- 解析审计工具；
- OCR 项检查工具；
- 视觉复核样本渲染工具；
- 坐标解析与审计回归测试；
- 可追溯的基线 JSON。

---

## 九、第六纪：质量治理与人工复核闭环

2026-07-23 的提交 `1fc6e4e` 将质量治理与人工复核闭环从在研转为已提交。本纪记录该阶段的建设内容与验证状态。

### 9.1 建设内容

该提交新增或扩展的模块显示，项目正在建设以下能力：

- 持久化解析任务；
- 单活动任务协调和软取消；
- 失败文件重试；
- SQLite 状态存储；
- 文件级任务进度；
- 质量状态和质量门禁；
- 问题确认与人工修正；
- 修正记录复用；
- 解析器签名；
- 当解析规则变化时让旧修正失效；
- 工作流结果快照与恢复；
- 审计事件记录；
- 导出前质量检查。

相关文件包括：

```text
app/services/job_service.py
app/services/review_service.py
app/services/state_store.py
app/services/workflow_persistence.py
core/identity.py
core/quality.py
core/signature.py
```

同时，`desktop_workflow`、`sidecar`、模型、解析核心和导出服务正在围绕这些能力扩展。

### 9.2 这一阶段的历史意义

如果这一阶段完成，空谷将不再只是一次性的“导入—解析—导出”工具，而会具备更完整的责任链：

```text
输入来源
  -> 解析任务
  -> 文件级进度
  -> 自动质量判断
  -> 人工复核与修正
  -> 修正复用
  -> 可信导出
  -> 审计留痕
```

这使项目从“识别程序”进一步走向“可追责、可恢复、可复核的业务工作流”。

### 9.3 史料谨慎说明

本章以 `1fc6e4e` 提交为证据：该提交于 2026-07-23 落地，并在 2026-08-08 治理基线验证中通过 91 项单元测试。真实样本审计、安装包级验证和断网运行仍未完成，因此本纪内容不能等同于正式发布声明；后续仍需补录数据库迁移策略、界面入口与发布版本。

### 9.4 PDF 结构旁路诊断

2026-08-09，项目研究 `firecrawl/pdf-inspector` 与同名 PyMuPDF 可视化工具后，在 Python sidecar 中新增独立适配层。该层只记录 PDF 类型、页数、逐页 OCR 建议、文本编码异常和复杂版式信号，并在现有 PDF.js 复核侧栏展示；第三方分类不直接决定课程块或 `accepted / needs_review / blocked`。

首轮实现固定 Python binding `pdf-inspector==0.2.6`，限制单文件 50 MB、100 页，失败时回退 PyMuPDF。PyInstaller 冻结构建已确认原生 `.pyd`、包元数据和 MIT 许可证进入 sidecar。仓库外 70 份、109 页真实样本的页级对账表明，PDF Inspector 建议 OCR 的 26 页与 PyMuPDF 的无文本层页面集合一致；该证据不涉及课程块、周次或节次人工真值，因此仍不得声称课表准确率提升，也未启用逐页 OCR 路由。

### 9.5 质量回归与隐私基线

2026-08-10，提交 `52c2c6a` 将项目版本升至 0.4.1，并补上真实样本治理的仓库内证据层。新增脱敏样本索引只保存合成成员标识、样本状态和必要哈希；真实 PDF 与真实姓名继续留在仓库外。人工真值文件先建立 35 组正常样本和 3 组阻断样本的空槽模板，评估器明确拒绝 `pending` 标注，因此该模板不能被误用为准确率证据。

同一提交新增代表性质量回归，覆盖正常接受、待复核、阻断、导出门禁、人工修正与解析签名绑定、规则变化后的旧修正失效，以及真值评估。测试和文档中的已知真实姓名、本机用户路径与样本库绝对路径被替换为合成标识或仓库外相对约定，并新增发布记录模板，要求后续发布记录安装、断网、全新用户目录、隐私和回滚验证。

提交前验证为：Python 全量测试 121 passed、2 warnings；Vite 前端构建通过；Cargo 对 konggu v0.4.1 检查通过。0.4.1 未构建安装包，也未完成人工课程块真值填写，因此当前状态是源码和质量基线已验证，不是正式发布。

---

## 十、架构演变图

```text
2026-06-02
V0.1 单体桌面程序
CustomTkinter + Python 核心
        |
        v
2026-06-04
分层架构 + 多入口探索
GUI / Services / Core + FastAPI Web
        |
        v
2026-06-20
Web-only 收缩
删除旧桌面端，保留本地 Web
        |
        v
2026-07-07
离线桌面重建
Tauri + Vite/TypeScript + Python sidecar
        |
        v
2026-07-09 至 07-23
识别可靠性建设
版式规则 + OCR 回退 + 真实样本审计
        |
        v
2026-07-23 起
质量治理与复核闭环
持久任务 + 质量门禁 + 人工修正 + 审计
        |
        v
2026-08-09
PDF 结构旁路诊断
逐页 OCR 建议 + 编码诊断 + 复核侧栏
```

---

## 十一、完整提交账

| 日期 | 提交 | 提交说明 | 历史作用 |
| --- | --- | --- | --- |
| 2026-06-02 | `dbdc8c4` | Prepare V0.1 release | 建立首个可追溯版本 |
| 2026-06-04 | `f5d6e25` | chore: update project configuration for V0.1 | 更新 V0.1 配置 |
| 2026-06-04 | `6df981f` | refactor: add application bootstrap layer | 建立应用启动层 |
| 2026-06-04 | `8a7b912` | refactor(services): introduce service layer for schedule workflow | 建立业务服务层 |
| 2026-06-04 | `3132679` | refactor(gui): split desktop launcher and GUI layer | 拆分旧桌面 GUI |
| 2026-06-04 | `390e23d` | feat(web): add local FastAPI web interface | 建立本地 Web 入口 |
| 2026-06-04 | `9bd7b81` | refactor(core): adapt schedule core for layered application | 核心适配分层架构 |
| 2026-06-04 | `32f1d53` | test: add coverage for V0.1 layered workflow | 增加分层回归测试 |
| 2026-06-04 | `bf899d4` | chore: remove obsolete legacy helpers | 清理旧辅助代码 |
| 2026-06-20 | `fb0ead7` | docs: refresh web UI assets | 更新 Web 视觉材料 |
| 2026-06-20 | `b425822` | chore: remove unused assets and placeholder packages | 清理重复素材和占位包 |
| 2026-06-20 | `69021fa` | refactor: remove desktop client | 进入 Web-only 阶段 |
| 2026-07-07 | `7985d61` | Convert Konggu to offline Tauri desktop app | 重建离线桌面主线 |
| 2026-07-08 | `9e1b3e5` | Release 0.3.0 | 发布 0.3.0 |
| 2026-07-08 | `382c0e1` | Remove unused generated resources | 发布后仓库清理 |
| 2026-07-23 | `d860f7c` | Solidify timetable parser audit baseline | 固化解析审计基线 |
| 2026-07-23 | `1fc6e4e` | Implement persistent review and trusted export workflow | 持久复核与可信导出工作流落地 |
| 2026-08-08 | `b7b4977` | Add governance layer docs and correct Konggu naming | 建立治理层与记忆系统，统一命名 |
| 2026-08-08 | `d039a6e` | docs: rewrite README in Chinese and bump version to 0.3.1 | README 全中文重写，版本升至 0.3.1 |
| 2026-08-08 | `90f5b65` | docs: polish README v0.4 and bump version to 0.3.2 | README v0.4 结构优化，版本升至 0.3.2 |

---

## 十二、关键决策录

### 决策一：空谷不是泛用课表软件

**结论：** 聚焦中英合办双课表合并和集体空课生成。

**原因：** 真实痛点不是个人看课表，而是负责人需要逐人、逐周、逐节核对中方和英方两套课表。

**影响：** 产品功能、README、输入契约、成员检查和输出结构都围绕这一场景收紧。

### 决策二：结果可信优先于识别覆盖

**结论：** 无法判断课表类型、身份或来源时拒绝猜测。

**原因：** 静默误判会直接污染空闲名单，进而影响真实排班。

**影响：** 未知类型、占位姓名、同内容异姓名和解析失败都必须显性进入错误或待复核状态。

### 决策三：优化程序，而不是手改结果

**结论：** 出现错误 Excel 时，修复解析器、输入预检、识别规则和回归测试，再从源课表重新生成。

**原因：** 手改一个文件不能解决下一批样本，也会破坏结果可追溯性。

**影响：** 真实样本库、审计 CSV、版式规则和回归基线成为解析迭代的主要依据。

### 决策四：文本层优先，离线 OCR 回退

**结论：** 先检查 PDF 文本层，只有在文本不足时才进入离线 OCR 或视觉回退。

**原因：** 原生文本层通常更快、更精确，OCR 应用于真正需要的样本。

**影响：** 识别链需要同时维护文本、坐标、OCR、视觉回退和完整度比较。

### 决策五：最终产品保持离线 Windows 桌面形态

**结论：** 使用 Tauri 桌面壳封装前端、Python sidecar 和 OCR 资源。

**原因：** 降低普通用户使用门槛，保护课表隐私，并减少对浏览器、本地服务和网络的依赖。

**影响：** 发布流程必须验证 sidecar、前端、Tauri、离线资源和安装包的完整链路。

### 决策六：删去已经确认无价值的历史负担

**结论：** 对重复素材、占位包、旧分支、无用生成物和废弃运行形态及时清理。

**原因：** 降低维护噪声，确保仓库反映真实产品状态。

**影响：** 项目历史中多次出现大规模删除提交；删除是有依据的产品治理，而非功能倒退。

### 决策七：变更必须与项目文档同步（含项目志）

**结论：** 任何进入正式交付的变更完成后，版本标识、README、变更记录、MEMORY 与 `PROJECT_CHRONICLE.md` 同步更新。

**原因：** 2026-07-23 建档后，治理层落地与 v0.3.1、v0.3.2 三次提交均未同步项目志，导致线上项目志停留在 v0.3.0 并缺失两段历史；文档不同步会破坏项目史的可信性。

**影响：** 每次变更需检查项目志补录；本志本次补录即按此执行。

---

## 十三、验证与发布账

| 时间 | 验证内容 | 结果 | 备注 |
| --- | --- | --- | --- |
| 2026-06-04 | 早期分层重构测试 | 47 passed, 1 warning | 历史测试集 |
| 2026-06-20 | Web-only 清理后测试 | 45 passed, 1 warning | 桌面测试随旧桌面端删除 |
| 2026-07-07 | 输入契约和桌面流程测试 | 49 passed, 4 warnings | 当时阶段记录 |
| 2026-07-07 | 前端构建 | 成功 | 使用 `npm.cmd run build:frontend` |
| 2026-07-07 | Windows 全量构建 | 成功 | sidecar + frontend + Tauri + NSIS |
| 2026-07-10 | 解析与审计回归 | 83 passed, 2 warnings | 真实样本强化后 |
| 2026-07-23 | 解析审计基线 | 73/76 自动通过 | 其余 3 份被正确标记为待复核 |
| 2026-08-08 | 治理基线测试 | 91 passed, 2 warnings | `.venv` pytest，提交 `1fc6e4e` |
| 2026-08-08 | 前端构建 | 成功 | `npm.cmd run build:frontend`（vite v8.1.3） |
| 2026-08-08 | 桌面层检查 | 通过 | `cargo check --manifest-path src-tauri\Cargo.toml --locked`（dev profile） |
| 2026-08-08 | 文档升级后测试 | 91 passed, 17 warnings | 提交 `b7b4977`（59.30s） |
| 2026-08-09 | 0.4.0 Python 全量测试 | 97 passed, 2 warnings | 功能提交 `e7ab4ef`；项目内独立 basetemp |
| 2026-08-09 | 0.4.0 前端构建与 cargo check | 通过 | Vite 8.1.3；konggu v0.4.0 |
| 2026-08-09 | PDF Inspector 真实样本旁路检查 | 70 份、109 页完成 | 26 个 OCR 建议页与 PyMuPDF 无文本层页集合一致；非准确率 |
| 2026-08-09 | 0.4.0 sidecar 冻结与冒烟 | 通过 | 原生扩展、SBOM、MIT LICENSE 已入包；未构建完整安装包 |

测试数量变化主要来自功能和用例的增删，不能简单用数字高低判断版本优劣。更重要的是每个阶段是否覆盖了当时的关键风险。

---

## 十四、走过但未保留的道路

### 14.1 旧 CustomTkinter 桌面端

它证明了早期业务流程可以在桌面运行，但界面、任务和业务逻辑耦合较重。2026-06-20 被完整移除，后来没有恢复，而是由 Tauri 桌面架构取代。

### 14.2 FastAPI 本地 Web 主入口

它推动了服务化和任务边界形成，但最终没有作为面向普通用户的交付形态保留。2026-07-07 转向 Tauri 时，Web 路由、模板和静态应用被删除。

### 14.3 对不明确输入做默认猜测

项目曾将未知课表类型默认当作某一类课表。随着可信度要求提高，这条策略被明确废止。

### 14.4 用单个输出文件掩盖解析问题

项目后期明确拒绝这种处理方式。一个错误输出应当成为解析器和流程改进的证据，而不是手工修补后被遗忘。

---

## 十五、当前状态快照

截至 2026-08-10：

- 仓库分支为 `kongguV0.1`；
- 源码版本标识为 `0.4.1`（`package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json` 一致），质量基线已由 `52c2c6a` 提交并验证；正式标签仍为 `v0.3.0`，0.4.1 未发布；
- 0.4.0 PDF 结构诊断提交为 `e7ab4ef`，0.4.1 质量与隐私基线提交为 `52c2c6a`；
- 质量治理、持久任务、人工复核与可信导出工作流已于 `1fc6e4e` 提交；
- 2026-08-10 已验证 121 项 Python 测试、前端构建和 cargo check；0.4.0 的 PDF Inspector sidecar 冻结证据继续有效；
- README 已移除过时测试徽章，测试数字按当次实测记录；
- 0.4.0 安装包已核验但落后于 0.4.1 源码；0.4.1 尚无对应安装包、断网运行和全新用户目录验证。

---

## 十六、下一阶段应补写的历史

以下内容完成后，应成为下一条正式纪事：

1. 为仓库外 70 份正式样本逐份填写课程块、周次和节次人工真值，并确定可脱敏固化的回归子集；
2. 补齐混合型、旋转页和乱码文本层真值，再决定是否启用逐页 OCR 路由；
3. 构建并验证与 `0.4.1` 版本标识一致的 Windows 安装包；
4. 记录安装包 SHA-256、大小、构建环境和验证机器；
5. 全新用户目录与断网运行验证；
6. 前端 chunk 体积优化（约 747 kB）按需进行。

注：持久任务、SQLite 状态存储、人工复核与修正复用、质量门禁接入导出、sidecar 控制与前端视图已由 `1fc6e4e` 提交，不再列入待补历史。

---

## 十七、后续日志书写规范

从本志建立之日起，每次有实质性迭代，建议在文首编年表增加一行，并在文末追加一条详细记录。

### 17.1 何时需要记载

以下变化应进入项目史：

- 新版本或安装包发布；
- 产品定位或范围变化；
- 运行形态、架构或主要依赖变化；
- 核心识别规则变化；
- 真实样本基线变化；
- 重大异常修复；
- 数据或隐私策略变化；
- 旧功能、旧分支或旧资产被删除；
- 测试、审计或质量门禁出现新的里程碑。
- 任何进入正式交付的变更（含文档、治理与记忆文件）完成后，项目志与版本标识、README、变更记录和 MEMORY 一起更新。

仅格式调整、无业务影响的代码整理可以留在 Git 提交中，不必扩写为大事记。

### 17.2 单次迭代记录模板

```markdown
## YYYY-MM-DD｜迭代标题

**版本：** x.y.z / 未发布  
**提交：** `abcdef0`  
**状态：** 已提交 / 已验证 / 在研  

### 背景

为什么需要这次迭代？来自真实业务、样本、错误还是产品决策？

### 本次变化

- 变化一
- 变化二
- 变化三

### 关键决策

做了什么取舍？为什么？

### 验证

- 测试：
- 真实样本：
- 构建：
- 安装包：

### 影响

对用户、数据、兼容性、架构和后续开发有什么影响？

### 遗留事项

- 尚未完成的事项
- 已知风险
```

### 17.3 正式发布记录模板

```markdown
## Konggu x.y.z

- 发布日期：
- Git 标签：
- Git 提交：
- 安装包：
- 文件大小：
- SHA-256：
- Windows 验证环境：
- 数据迁移：
- 主要新增：
- 主要修复：
- 已知问题：
- 回滚方式：
```

---

## 结语

空谷的历史并不是一条笔直路线。

旧桌面端被拆分，Web 端被建立；桌面端又被彻底删除，随后以完全不同的 Tauri 架构重建。项目从强调功能跑通，转向强调输入契约、真实样本、识别审计和质量复核。

这些转折并不互相否定。早期桌面端验证了业务，Web 化推动了分层，Web-only 迫使项目做减法，Tauri 重建解决了最终交付，真实样本审计则让空谷开始对结果负责。

截至 2026-07-23，空谷已经完成了从“多人课表处理程序”到“离线双课表合并与可信空课生成工具”的核心转变。下一段历史，将取决于质量治理和人工复核闭环能否正式落地。
