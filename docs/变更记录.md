# 空谷项目变更记录

## 2026-08-08

### 类型

项目治理层落地与基线验证。

### 变更内容

- 按「空谷优化包」（2026-08-06 生成）落地治理层，不逐字照搬；原始包已删除，不随仓库保留。
- 以合并版 `AGENTS.md` 替换原有工作指令；保留受众分层表达、`npm.cmd`/PowerShell 细节与 Cargo 指向 `src-tauri` 清单，并将参考资料目录适配为 `resources/reference/`。
- 建立 `MEMORY/` 四文件动态记忆系统（`MEMORY.md`、`决策点.md`、`待办.md`、`事实.md`），新增 D-009 治理落地决策。
- 建立 `resources/reference/` 参考库及资料登记表。
- `docs/` 新增 `项目说明.md`、`状态与质量门禁.md`、`发布检查清单.md`、`变更记录.md`。
- `tests/` 新增 `README.md` 与 `回归样本登记表.md`。
- `README.md` 新增「项目治理与协作」章节，并将测试章节数字更新为 2026-08-08 验证结果。

### 验证结果（2026-08-08，分支 kongguV0.1，提交 1fc6e4e）

- 单元测试：`91 passed, 2 warnings`（`.venv\Scripts\python.exe -m pytest -q`，27.03s）。
- 前端构建：`npm.cmd run build:frontend` 通过（vite v8.1.3，40 modules）；存在 index chunk 约 745 kB 的体积提示，非阻断。
- 桌面层检查：`cargo check --manifest-path src-tauri\Cargo.toml --locked` 通过（dev profile，1.56s）。
- 未验证项：脱敏真实样本回归（外部样本库未挂载）、完整安装包构建、全新用户目录与断网运行（发布级，需后续单独授权）。

### 证据状态

- 文件已生成并通过自检。
- 基线验证结果见上；详细事实见 `../MEMORY/事实.md`。

### 遗留事项

参见 `../MEMORY/待办.md`。

## 说明

- 2026-08-06 的「空谷优化包」是本次治理落地的规范来源，原始包未随仓库保留。
- 更早的项目沿革（V0.1 至 0.3.0、解析审计基线等）记录在 `PROJECT_CHRONICLE.md`，不在本文件重复抄写。
