# 空谷项目长期工作指令

## 1. 文件定位与适用范围

本文件是空谷项目的长期工作规范，统一规定项目定位、AI 协作方式、任务权限、事实核验、工程流程、隐私保护、质量门禁、测试验证、文档同步和交付要求。

本文件适用于以下工作：

- 产品定位、需求分析和方案评审；
- 课表输入预检、文本提取、OCR、版式识别和结构化解析；
- 多人成员占用合并、空闲计算、质量判断和人工复核；
- 桌面端、工作流服务、配置、持久化和导出模块开发；
- Excel、Word、PDF、界面、安装包和发布说明交付；
- 测试、真实样本回归、构建、发布和项目文档维护。

### 1.1 指令与事实优先级

发生冲突时，按以下顺序处理：

1. 用户当前明确要求及本轮提供的源材料；
2. 本文件中适用于当前任务的项目规则；
3. 当前工作区代码、配置、测试和实际运行结果；
4. 经过脱敏的真实样本、人工标注、审计结果和回归基线；
5. `MEMORY/` 中经过核验的当前记录；
6. Git 提交、标签、构建记录和可核验产物；
7. `resources/reference/`、README、项目史、设计说明和历史报告；
8. 优秀样例的结构、语言和视觉做法。

模板可以决定对应交付物的版式，但不得让模板中的旧事实覆盖当前证据。历史文档与当前实现冲突时，必须明确指出冲突，不得静默选择其中一方。

### 1.2 动态信息边界

以下内容不得长期固化在本文件中：

- 当前版本号、分支名和提交号；
- 当前依赖、模型或框架版本；
- 当前测试数量、样本数量和性能指标；
- 当前安装包名称、文件大小和哈希；
- 短期开发计划、具体缺陷和阶段性进度。

这些内容应写入 `MEMORY/事实.md`、`MEMORY/待办.md` 或对应发布记录。

---

## 2. 项目身份与名称含义

空谷是青禾计划孵化的课表协作产品。

对外统一表述为：

> 空谷是青禾计划孵化的课表协作产品。“空”指空课表，“谷”承接青禾从禾苗生长为谷物的意象。

必须先说明空谷与青禾计划的归属关系，再解释名称。

禁止使用“空课汇聚成谷”等脱离青禾计划项目归属的解释，不得自行扩展未经用户确认的象征含义。

---

## 3. 产品目标、用户与范围

### 3.1 核心目标

空谷始终围绕“多人中方、英方课表汇总与可信空课生成”开展工作，不得泛化为普通个人课表工具。

项目应当：

- 服务需要组织值班、活动、会议、培训和志愿服务排班的学生组织负责人及协作团队；
- 将分散、异构、含个人信息的课表材料转化为可预览、可复核、可追溯、可导出的集体空闲结果；
- 减少逐人、逐周、逐节核对课表的重复劳动；
- 保证错误可见、结果可信、来源可追溯、修正有记录；
- 坚持离线 Windows 交付和本地数据处理；
- 以可信结果和完整责任链优先于功能数量、识别覆盖率和表面自动化程度。

### 3.2 核心范围

核心范围包括：

- 中方、英方课表导入；
- 文件类型、身份、课表类别和完整性预检；
- PDF 文本层提取；
- 必要时使用本地 OCR 或视觉回退；
- 版式识别和课程块结构化；
- 成员占用合并和空闲计算；
- 结果预览、异常提示、人工复核和修正记录；
- 汇总视图、明细、来源、问题和处理记录导出；
- 测试、回归、构建、安装和离线运行验证。

### 3.3 非核心范围

除非用户明确扩大范围，不主动加入：

- 云同步；
- 在线账号体系；
- 长期成员数据库；
- 报名、考勤和审批系统；
- 与课表排班闭环无关的复杂统计报表；
- 未经授权的数据上传、云端识别或外部处理；
- 为追求功能数量而增加的组织管理模块。

---

## 4. 基本协作原则

### 4.1 先核验，后判断

- 不确定的事情如实说明，不得推测补齐。
- 事实不足时使用“待确认：缺少……”或“未验证：需要……”。
- 工作区存在实现不等于已提交，已提交不等于已验证，已验证不等于已发布。
- 自动解析成功不等于结果准确。
- 测试通过数、自动接受率和样本数量不得包装成未经真值验证的准确率。

### 4.2 不盲目附和

- 发现需求、方案、数据或既有决策存在矛盾时，明确指出。
- 发现不合理方向时，说明理由、影响和替代方案。
- 遇到方案选择时，优先给出“首选 / 可以接受 / 不建议”及理由。
- 不得为了迎合用户而省略风险、错误或未验证状态。

### 4.3 结论必须可复核

输出应展示：

- 最终结论；
- 关键依据；
- 实际执行步骤；
- 修改范围；
- 验证结果；
- 已知限制；
- 遗留事项。

不要求展示冗长的内部思考过程，但必须保留足以复核结论的证据链和操作记录。

### 4.4 优先复用现有资源

提出新方案或开始修改前，检查：

- `MEMORY/` 中的当前状态、历史决策和待办；
- `resources/reference/` 中的模板、样例和历史材料；
- 当前代码、配置和测试；
- 已有异常样本、审计结果和回归基线；
- 已有文档、脚本和构建流程。

已有可复用内容时，不重复创建功能相同但口径不一致的材料。

---

## 5. 任务分级与启动规则

### 5.1 A 类：只读或低风险任务

包括：

- 阅读、解释、总结和审查；
- 错误诊断和方案比较；
- 代码、文档或流程检查；
- 不修改文件的规划和建议。

执行规则：

- 直接开展，不修改工作区；
- 先给结论，再说明依据；
- 不需要因步骤较多而单独等待确认；
- 无法核验的内容明确标记。

### 5.2 B 类：普通修改任务

包括：

- 修复单个模块；
- 调整解析规则、配置或界面文案；
- 增加测试和回归用例；
- 更新说明文档；
- 生成普通交付物。

执行规则：

1. 用一句话明确问题、服务对象和成功标准；
2. 列出简要任务清单；
3. 检查 Git 状态、MEMORY、`resources/reference/`、当前实现和相关测试；
4. 用户已明确要求修改时，可以直接执行；
5. 只修改本次任务必要范围；
6. 完成后报告修改内容、验证结果和遗留事项。

### 5.3 C 类：高风险或不可逆任务

包括：

- 删除、迁移、大范围重写或覆盖文件；
- 修改核心架构、数据契约、隐私策略或发布流程；
- 覆盖用户已有未提交改动；
- 暂存、提交、推送、合并、创建发布或正式发布；
- 对外发送材料或以用户身份沟通；
- 处理可能造成个人课表数据泄露的操作；
- 批量处理且难以回滚的任务。

执行规则：

- 先展示准确方案；
- 说明影响范围、不可逆部分、风险和恢复方式；
- 未获得明确授权，不得执行；
- 授权后也只能处理本次任务范围，不得顺带修改其他内容。

---

## 6. 项目记忆与文件管理

### 6.1 固定目录

`MEMORY/` 包含：

- `MEMORY.md`：当前状态总览和索引；
- `决策点.md`：已确认选择、原因、替代方案和影响；
- `待办.md`：尚未完成或待确认事项；
- `事实.md`：当前可核验的动态事实。

`resources/reference/` 用于保存（本项目顶层 `resources/` 已被运行时 OCR 资源占用，参考库固定使用本子目录）：

- 模板；
- 脱敏样例；
- 视觉规范；
- 历史材料；
- 外部参考；
- 人工标注规范；
- 不属于代码和正式文档的辅助资料。

`docs/` 用于保存：

- 项目说明；
- 架构和数据流；
- 输入契约；
- 状态与质量门禁；
- 测试、发布和变更记录；
- 用户说明和故障处理。

### 6.2 内容归属

- “始终……”“禁止……”等长期规则写入 `AGENTS.md`。
- 已确认选择及原因写入 `MEMORY/决策点.md`。
- 尚未完成、待验证或待确认事项写入 `MEMORY/待办.md`。
- 版本、分支、测试、构建、样本和发布状态写入 `MEMORY/事实.md`。
- 模板、样例和历史参考放入 `resources/reference/`。
- 正式说明和过程记录放入 `docs/`。

### 6.3 持续记录

开始任务时，先读取与任务相关的 MEMORY 内容，无需主动复述全部记录。

协作过程中：

- 新的关键决策应及时写入 `决策点.md`；
- 新的未解决问题应及时写入 `待办.md`；
- 新的可核验状态应及时写入 `事实.md`；
- 待办完成后应从未完成区域移除，并在变更记录中保留必要结果；
- 一段工作收尾、话题切换或状态发生较大变化时，同步更新 `MEMORY.md`。

不得把已写入 `AGENTS.md` 的规则重复抄入 MEMORY。

---

## 7. 工作区保护与操作权限

### 7.1 修改前检查

修改前必须：

- 检查 Git 状态；
- 识别已修改、未跟踪、已暂存和忽略文件；
- 识别当前分支、相关提交和任务范围；
- 保留用户已有改动；
- 确认目标文件和可恢复方式。

### 7.2 禁止的破坏性操作

未经明确授权，不得执行：

- `git reset --hard`
- `git checkout --`
- `git restore` 覆盖用户改动
- `git clean`
- 强制推送
- 大范围删除、覆盖或批量重命名
- 删除无法确认来源的缓存、样本或构建产物

### 7.3 提交与发布

暂存、提交、推送、合并、打标签和发布必须获得用户明确授权。

提交范围必须只包含本次任务相关文件。不得顺带提交用户原有改动、临时文件、真实课表或敏感材料。

---

## 8. 输入材料与预检

### 8.1 输入检查清单

开始解析前检查：

- 文件类型、扩展名和可读性；
- 文件名中的成员身份和课表类别信息；
- 中方、英方课表配对是否完整；
- 是否存在缺失、重复或损坏文件；
- 是否存在同内容异身份；
- 是否存在占位姓名、测试信息或无效命名；
- 文件名与正文身份是否冲突；
- 是否能可靠判断课表版式；
- PDF 是否存在可用文本层；
- 是否包含真实姓名、路径、课程和其他个人信息；
- 历史缓存、标注和修正是否仍与当前源文件及解析器匹配。

### 8.2 失败处理

身份、课表类别、文件来源或成员对应关系无法可靠确认时：

- 不得静默猜测；
- 标记为 `needs_review` 或 `blocked`；
- 明确指出缺少的信息；
- 给出最小补证方法；
- 在问题解决前不得生成正式结果。

### 8.3 文本与 OCR 优先级

- 优先使用 PDF 文本层；
- 文本不足或不可用时才使用本地 OCR 或视觉回退；
- 禁止为方便而默认对所有文件执行 OCR；
- 禁止未经授权使用云端识别；
- OCR 结果必须经过版式、字段和逻辑校验，不得直接视为真值。

---

## 9. 隐私与数据保护

- 真实课表应保存在版本库外。
- 公开仓库、公开日志、测试报告和基准文件不得包含真实姓名、原始路径、完整课表或完整课程文本。
- 仓库内仅保存必要的脱敏标识、内容哈希、规则、期望结果和基线摘要。
- 禁止引入运行时模型下载、隐式联网和未经授权的数据上传。
- 新增依赖时必须说明是否联网、是否产生遥测、是否增加安装包体积和是否影响离线运行。
- 日志只保留定位问题所需字段，默认隐藏姓名、路径和课程正文。
- 临时审计文件、缓存和导出结果必须与正式代码和基线清晰分离。
- 发布前执行隐私检查，确认安装包、日志、示例和文档中不存在敏感信息。

---

## 10. 默认工程工作流

1. 明确问题、服务对象、范围和成功标准。
2. 读取相关 MEMORY、`resources/reference/`、历史决策和异常记录。
3. 检查 Git 状态、当前实现、配置、测试和可执行行为。
4. 执行输入身份、类型、完整性、重复、冲突和隐私预检。
5. 沿数据链逐层定位问题：

```text
源文件
→ 文本层或本地 OCR
→ 版式识别
→ 结构化课程块
→ 成员占用合并
→ 空闲计算
→ 质量判断
→ 人工复核
→ 导出
```

6. 实施最小、可复用的程序级改进，同步更新配置、异常分类和回归用例。
7. 从原始课表重新生成结果，不沿用未经验证的旧缓存或手工修改文件。
8. 执行与改动风险匹配的单元测试、真实样本回归、构建、界面、离线资源和发布包验证。
9. 对待复核问题执行原文对照、人工修正和原因记录；修正后重新计算受影响结果。
10. 通过质量门禁后生成正式成果。
11. 更新 MEMORY、文档、验证状态、已知限制和遗留事项。

---

## 11. 工程与产物边界

### 11.1 模块职责

保持桌面界面、工作流服务、解析核心、配置、持久化、导出和测试之间的职责边界。

除非任务明确要求重构：

- 不把新逻辑继续堆入单一核心文件；
- 不让界面层承担解析规则；
- 不让导出层修补上游数据错误；
- 不用日志代替用户可理解的状态提示；
- 不让缓存或生成物成为唯一事实来源。

### 11.2 修复原则

出现错误结果时，优先判断问题属于：

- 输入预检；
- 身份识别；
- 课表类型；
- 文本提取；
- OCR；
- 版式识别；
- 课程结构化；
- 占用合并；
- 空闲计算；
- 质量规则；
- 人工修正复用；
- 导出逻辑。

必须修复可复用的程序、配置、规则或回归体系，不得只手工修改单个 Excel 或输出文件掩盖缺陷。

### 11.3 依赖管理

新增依赖前说明：

- 必要性；
- 现有依赖为何不能满足；
- 离线运行影响；
- 安装包体积影响；
- 安全和隐私影响；
- 可行替代方案。

优先复用当前依赖和本地资源。

### 11.4 人工修正

人工修正必须：

- 记录修正前后内容、原因和操作者；
- 与源文件内容哈希和解析器签名关联；
- 在解析器、版式、OCR、配置或规则变化后重新评估；
- 不得在来源变化后静默复用旧修正。

---

## 12. 事实状态与质量门禁

### 12.1 事实状态

统一使用：

- `已提交`：存在可追溯提交；
- `已验证`：存在当前测试、审计、构建或产物证据；
- `在研`：工作区存在实现但尚未完成交付验证；
- `历史形态`：过去存在但已被替换或删除；
- `待确认`：现有材料不足；
- `未验证`：已有主张但本轮未执行验证。

### 12.2 解析质量状态

统一使用：

- `accepted`：输入完整，关键字段通过规则或人工核验，不存在阻断问题；
- `needs_review`：结果可继续处理，但身份、课程块、周次、时间或版式存在需要人工确认的低置信项；
- `blocked`：缺少必要文件、身份冲突、类型无法确认、解析失败或存在影响正式结果的错误。

### 12.3 门禁规则

- 存在 `blocked` 时不得生成正式结果。
- 未解决的 `needs_review` 不得标记为正式完成。
- 不得通过“确认”按钮直接放行阻断错误，必须修复源文件、身份信息或解析结果。
- 只有人工核验的真值数据才能支持准确率声明。
- 自动接受率、测试通过数和样本数量只能作为带时间点的运行证据。
- 任何必要验证未运行或失败时，明确标记“未验证”或“验证失败”，并说明影响。

---

## 13. 分层验证要求

### 13.1 解析器与质量规则

修改解析器、成员检查、质量规则或导出逻辑时：

- 运行相关 Python 单元测试；
- 使用对应的脱敏真实样本回归；
- 检查正常、边界、冲突、低置信和失败场景；
- 不得仅用人工构造数据证明真实 PDF 兼容性；
- 核对结构化结果、占用合并、空闲计算和问题状态。

### 13.2 前端与桌面端

修改前端时：

- 使用当前 `package.json` 定义的脚本完成构建；
- 检查正常、待复核、阻断、失败和桌面环境不可用状态；
- 在 Windows PowerShell 中优先使用项目兼容的命令形式（本项目前端构建优先使用 `npm.cmd`）；
- 检查文件级状态、错误说明和下一步操作是否清晰。

修改 sidecar、Tauri 或 Rust 层时：

- 验证命令协议和前端集成；
- 运行对应 Cargo 检查（本项目为 `cargo check --manifest-path src-tauri\Cargo.toml --locked`）；
- 明确指向正确的项目清单；
- 检查离线资源和进程生命周期。

### 13.3 文档与表格

交付 Excel 时检查：

- 工作表命名和结构；
- 汇总视图与明细关系；
- 筛选、公式、冻结窗格和字段说明；
- 成员完整性、来源、问题和人工修正记录；
- 乱码、裁切、空白页和不可读表格；
- 实际打开和渲染结果。

交付 Word 或 PDF 时检查：

- 标题层级、字体、分页、表格和图片；
- 页眉页脚、页码和目录；
- 乱码、重叠、裁切和异常分页；
- 实际渲染，而非只检查源码或文本提取。

### 13.4 发布候选包

发布候选包至少验证：

- Python 测试；
- 前端构建；
- Cargo 或桌面层检查；
- sidecar 和离线资源完整性；
- 完整安装包构建；
- 发布包静态检查；
- 全新用户目录运行；
- 断网运行；
- 安装、启动、导入、预览、复核、导出和卸载关键流程。

---

## 14. 文档、版本与发布同步

- 行为、输入契约、异常状态或用户流程变化时，同步检查代码、配置、测试、README、界面文案和使用说明。
- 重大产品、架构、解析规则、隐私策略或质量基线变化完成验证后，再更新项目史。
- 不得提前将“在研”写成“已完成”或“已发布”。
- 发布前核对应用版本、安装包版本、Git 提交、资源清单和公开说明。
- 正式发布记录必须包含：
  - 产物名称；
  - 版本号；
  - 对应 Git 提交；
  - 文件大小；
  - SHA-256；
  - 构建和验证环境；
  - 安装与断网验证；
  - 已知问题；
  - 回滚方式。
- 所有数字、版本和指标必须注明时间点和证据来源。

---

## 15. 输出结构与语言标准

### 15.1 默认输出顺序

根据任务裁剪，但保持以下核心逻辑：

1. 结论或最终建议；
2. 问题、受众与使用价值；
3. 输入条件和处理流程；
4. 实际结果或已完成变化；
5. 异常、风险和证据状态；
6. 验证结果；
7. 使用方式、影响和遗留事项。

### 15.2 表达要求

- 默认使用准确、自然、专业的中文。
- 保留必要的英文技术名词、状态名和代码标识。
- 使用直接、可执行的表达。
- 先给结论，再说明条件、证据和限制。
- 避免空泛愿景、宣传口号、夸张形容、重复总结和明显的 AI 腔。
- 每个段落只承担一个主要观点。
- 每个列表项表达一项可执行要求。
- 保持术语、日期、文件名、字段和状态名称一致。
- 只有在需要横向比较或重复字段查阅时使用表格。
- 用户界面必须显示当前阶段、文件级状态、可行动的错误说明和下一步。
- 根据受众调整表达：面向普通使用者先讲痛点、用途和操作；面向开发者再讲数据流、模块边界和验证方法；面向发布审核者重点提供版本、证据、风险和回滚信息。

### 15.3 交付物结构

Excel 成果优先包含：

- 面向排班的汇总视图；
- 可筛选的空课明细；
- 成员及课表完整性检查；
- 课程占用与来源明细；
- 解析问题和处理状态；
- 人工修正历史；
- 文件处理记录；
- 使用说明。

---

## 16. 禁止事项

### 16.1 事实与质量

- 禁止在身份、课表类型、来源或解析结果不明确时静默猜测。
- 禁止隐藏缺失、冲突、解析失败、低置信或待复核状态。
- 禁止将自动接受率、测试通过数或样本数量表述为未经真值验证的准确率。
- 禁止把旧版本、旧提交、旧安装包或历史截图描述为当前成果。
- 禁止编造测试、构建、审计、引用、路径、用户反馈和发布结论。

### 16.2 工程与数据

- 禁止手工修改单个 Excel 来掩盖解析器或流程缺陷。
- 禁止只更新文档口径而不核对代码、配置、测试和实际行为。
- 禁止把缓存、构建目录或临时结果当作唯一事实来源。
- 禁止未经授权上传、联网处理或公开保存个人课表材料。
- 禁止在公开测试、日志和基准文件中保存真实姓名、原始路径、原始课表或完整课程文本。
- 禁止擅自执行可能破坏工作区的 Git 命令。

### 16.3 交付

- 禁止用后台日志、异常堆栈或模糊错误码直接代替用户提示。
- 禁止交付存在裁切、错位、乱码、公式错误、空白页、不可读表格或无操作指引的成果。
- 禁止在必要验证未运行或失败时声称整体通过。
- 禁止在未核验版本、提交、安装包和哈希时作正式发布声明。

---

## 17. 交付前检查清单

- [ ] 是否明确服务对象、真实问题、任务范围和成功标准？
- [ ] 是否读取相关 MEMORY、`resources/reference/` 和历史决策？
- [ ] 是否检查当前代码、配置、测试和 Git 状态？
- [ ] 是否区分已提交、已验证、在研、历史形态、待确认和未验证？
- [ ] 是否完成文件类型、身份、课表类别、完整性、重复和冲突预检？
- [ ] 是否优先使用文本层，并只在必要时使用本地 OCR 或视觉回退？
- [ ] 是否修复可复用的程序、配置或规则，而非只修一个输出文件？
- [ ] 是否从源材料重新生成并检查结果？
- [ ] 是否对关键异常执行人工复核并记录原因？
- [ ] 是否阻止未解决的 `blocked` 和 `needs_review` 进入正式导出？
- [ ] 是否执行与修改风险相匹配的测试、样本回归、构建和离线验证？
- [ ] 是否核验版本、数字、状态、产物和发布声明？
- [ ] 是否清除或脱敏个人信息、绝对路径和原始课表内容？
- [ ] 是否保持代码、配置、测试、文档、界面和输出口径一致？
- [ ] 是否完成 Word、PDF、Excel 或界面的实际渲染检查？
- [ ] 是否更新 MEMORY、变更记录和遗留事项？
- [ ] 是否给出可直接使用的最终成果、验证结论和已知限制？
