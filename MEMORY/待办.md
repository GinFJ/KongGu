# 空谷项目待办

> 本文件仅保留尚未完成、待验证或待确认事项。任务完成后，应从未完成区域移除，并在 `docs/变更记录.md` 或相关记录中保留结果。

## P0：建立当前基线

- [x] 确认仓库路径、分支（`kongguV0.1`）和 0.4.1 质量基线提交（`52c2c6a`）。2026-08-10
- [x] 核验版本（0.3.0）和技术栈（Tauri 2 + Vite/TypeScript + Python sidecar）。2026-08-08
- [x] 执行单元测试、前端构建和 cargo check 并记录结果。2026-08-08
- [x] 核验当前安装包、版本号、对应提交、文件大小和 SHA-256。2026-08-09
  - **旧安装包 Konggu_0.3.0_x64-setup.exe**：276 MB，SHA-256 `d6576fad`……构建于 2026-07-23（提交 `1fc6e4e`），版本 0.3.0。
  - **新安装包 Konggu_0.4.0_x64-setup.exe**：283 MB，SHA-256 `41c8c7d429642544b4e9b0bd272061e805c5372c767d0c58961722c0366f68ce`，构建于 2026-08-09 22:53（提交 `85f8461`），版本 0.4.0，与源码一致。
- [x] 找到脱敏真实样本库并执行回归（`config/reference_library.json` 登记 70 份）。2026-08-09：挂载仓库外 `reference_library`，运行 `tools/precache_reference_library.py`，总数 70，已有缓存 3，文本可用 52，新增 OCR 15，解析缓存 70，失败 0，耗时 53.8 分钟。

## P1：建立质量和回归基础

- [x] 建立脱敏真实样本登记表和内容哈希（`tests/回归样本登记表.md`）。2026-08-10：35 组完整配对组 + 8 份阻断样本全部编入 S-CN-01~35、S-BLK-01~08，SHA-256 取自外部清单。
- [x] 建立可复核真值模板并区分自动接受率与准确率。2026-08-10：`docs/baselines/ground-truth-2026-08-10.json` 含 35 组 + 3 组阻断的空槽待填模板，`evaluate_samples` 拒绝 pending 标注。
- [ ] 逐份人工填写并复核 35 组课程块、周次和节次真值；完成前不得声明真实课表准确率。
- [x] 处置 3 组缺少中方或英方课表的成员材料，并重新执行配对门禁。2026-08-10：移除不完整成员（成员X·成员Y·成员Z→阻断区）。
- [x] 对 1 组同内容异姓名争议执行保守处置。2026-08-10：成员A和成员B英方课表保留为仅单方，争议中方继续阻断。
- [ ] 若需恢复成员A/成员B完整配对，补证争议中方课表的真实归属后再重新纳入。
- [x] 复用现有 OCR 缓存分批复跑 20 份图片型样本，确认审计进程正常退出。2026-08-10：17/17 accepted 图片型全部 OK，平均 ~100ms/份，进程正常退出无超时。
- [x] 为正常、待复核、阻断和解析失败建立代表性回归用例。2026-08-10：`tests/test_regression_quality_gates.py` 24 个用例覆盖 accepted/needs_review/blocked/导出门禁/真值评估/修正绑定。
- [x] 检查人工修正是否与源文件哈希和解析器签名关联。2026-08-10：`CorrectionRecord` 强制 `source_hash` + `parser_signature` 字段；`StateStore.corrections_for_source` 按签名比对 stale；DB 有 `corrections_lookup` 索引。
- [x] 检查解析器、配置或版式变化后旧修正是否会失效。2026-08-10：`mark_other_signatures_stale` 在签名变化时将旧修正标记为 stale；`corrections_for_source` 返回 stale=True；已修正块只应用 `parser_signature` 完全匹配且 stale=False 的修正。
- [x] 检查正式导出是否严格阻止未解决的 `blocked` 和 `needs_review`。2026-08-10：`assert_export_allowed` 在 blocked 抛出"存在阻断错误"、needs_review 抛出"仍有未确认问题"；error 级 issue 不可通过 confirm 放行。
- [x] 用仓库外 70 份、109 页真实样本对账 PDF Inspector 与 PyMuPDF 的逐页文本层判断。2026-08-09：两者识别出的 26 个无文本层 / 建议 OCR 页面集合完全一致，未发现编码异常；该结果不是课程块准确率。
- [ ] 为 PDF 类型分类、编码异常和复杂版式建立人工真值；当前页级对账不能证明这些通用诊断在课表语义上的准确率。
- [ ] 对账通过后再评估逐页 OCR 路由；若启用，必须同步升级 OCR/解析缓存版本并覆盖混合型、旋转页和乱码文本层回归。

## P1：隐私与发布检查

- [x] 检查仓库、日志、测试、文档和示例中是否存在真实姓名、原始路径或完整课表内容。2026-08-10：修复测试代码（4 文件）、reference_library_samples.json（35+ 姓名脱敏）、回归登记表、MEMORY、异常矩阵等文档中的真实姓名；`config/reference_library.json` 仅保留仓库外相对路径约定。
- [x] 检查是否存在运行时模型下载、隐式联网或未经授权的数据上传。2026-08-10：`KONGGU_ALLOW_OCR_MODEL_DOWNLOAD` 在 bootstrap 和 offline_resources 两处主动 pop；仅 `tools/prepare_offline_resources.py`（构建脚本）使用 `urllib.request`；Rust 侧无 HTTP 依赖；前端无 fetch/XHR；Tauri 权限仅允许 sidecar spawn。
- [x] 建立发布候选包的全新用户目录和断网验证流程。2026-08-10：`docs/发布检查清单.md` 已含全新用户目录和断网验证检查项，发布记录模板 `docs/releases/release-template.md` 已含断网终检项；实际断网安装验证需在构建新安装包后执行。
- [x] 建立正式发布记录模板并核验回滚方式。2026-08-10：`docs/releases/release-template.md` 已创建；回滚方式：当前仓库有 git tag `v0.3.0`，可重新构建该标签对应的安装包作为回滚目标。

## P1：阿谷虚拟形象

- [x] 确认角色身份、名称含义、三项固定识别和界面使用边界。2026-08-11
- [x] 建立四张源图、确定性资产构建脚本、尺寸与 SHA-256 清单。2026-08-11
- [x] 接入启动页、未导入欢迎页、可信完成状态、应用图标、README 和品牌规范。2026-08-11
- [x] 完成 Python 测试、前端构建、Cargo 检查和 1080×820 本地界面检查。2026-08-11
- [ ] 构建并核验 0.4.2 Windows 安装包，检查真实桌面启动画面、系统图标、断网运行、全新用户目录和卸载流程。

## 待用户确认

- [ ] 当前需要优先支持的中方、英方课表样式。
- [ ] 当前正式导出需要包含的工作表和字段。
- [ ] 参考资料库（`resources/reference/`）首批登记内容。
- [ ] 从正式样本库中确定可脱敏固化的 PDF Inspector 回归子集。
