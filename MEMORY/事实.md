# 空谷项目事实

> 本文件只记录当前可核验的动态事实。所有版本、数字和指标均注明日期、提交和验证环境；证据不足时标记「待确认」或「未验证」。

## 一、已确认事实

| 项目 | 当前事实 | 状态 | 证据说明 |
|---|---|---|---|
| 项目归属 | 空谷是青禾计划孵化的课表协作产品 | 已确认 | AGENTS.md 长期规则 |
| 核心任务 | 多人中方、英方课表汇总与可信空课生成 | 已确认 | AGENTS.md 长期规则 |
| 主要用户 | 学生组织负责人及需要协作排班的团队 | 已确认 | AGENTS.md 长期规则 |
| 主要场景 | 值班、活动、会议、培训和志愿服务排班 | 已确认 | AGENTS.md 长期规则 |
| 交付原则 | 离线 Windows、本地处理、隐私保护 | 已确认 | AGENTS.md 长期规则 |
| 质量原则 | 可信结果和完整责任链优先 | 已确认 | AGENTS.md 长期规则 |
| 质量状态 | `accepted / needs_review / blocked` | 已确认 | AGENTS.md 长期规则 |
| 解析顺序 | 文本层优先，必要时本地 OCR 或视觉回退 | 已确认 | AGENTS.md 长期规则 |

## 二、2026-08-08 核验的当前事实

| 项目 | 当前事实 | 状态 | 证据说明 |
|---|---|---|---|
| 默认分支 | `kongguV0.1` | 已验证 | `git branch --show-current` |
| 当前提交 | `90f5b65`（docs: polish README v0.4 and bump version to 0.3.2） | 已验证 | `git rev-parse HEAD` |
| 应用版本 | 0.3.2 | 已验证 | package.json / src-tauri/tauri.conf.json / src-tauri/Cargo.toml |
| 技术栈 | Tauri 2 + Vite/TypeScript 前端 + Python sidecar（PyMuPDF、PaddleOCR、pandas、openpyxl、PyInstaller） | 已验证 | 源码、package.json、requirements.txt |
| Python 环境 | Python 3.12.4，pytest 9.1.1（.venv） | 已验证 | 2026-08-08 环境检查 |
| 单元测试 | 91 passed, 17 warnings（59.30s，提交 `b7b4977` 实测） | 已验证 | 2026-08-08 `python -m pytest -q` |
| 前端构建 | 通过（vite v8.1.3，40 modules，343ms）；存在 chunk 体积提示 | 已验证 | 2026-08-08 `npm.cmd run build:frontend` |
| 桌面层检查 | 通过（dev profile，1.56s） | 已验证 | 2026-08-08 `cargo check --manifest-path src-tauri\Cargo.toml --locked` |
| 仓库外真实样本库 | 受控 `reference_library`；70 份 PDF，中方 35、英方 35，共 35 组完整成员对 | 已验证 | 2026-08-09：从 78 份源 PDF 中按解析、重复内容、身份和配对门禁整理；SHA-256 对账失败 0、正式库重复内容 0 |
| 阻断样本 | 8 份 PDF：占位文件 1、同内容异姓名 2、因缺少可用中方或英方配对而暂扣 5；另有非 PDF 附件 1 份 | 已验证 | 2026-08-09 仓库外 `01_审计结果/样本整理清单.csv` 与 `阻断错误清单.csv` |

## 三、待核验事实

| 项目 | 当前记录 | 状态 | 补证方法 |
|---|---|---|---|
| 真实样本库回归 | 70 份 PDF 全部完成预缓存和解析回归：52 份本轮文本层解析、15 份本轮 OCR、3 份复用已有缓存，共 70 份。失败 0，耗时 53.8 分钟。 | ✅ 已验证 | 2026-08-09 `tools/precache_reference_library.py`，仓库外真实样本库 |
| 人工真值基线 | 35 组完整成员对尚未逐份人工核对课程块、周次和节次；`auto_accepted` 不能作为准确率 | 待确认 | 按 tests/回归样本登记表.md 建立脱敏登记与人工真值 |
| 图片型审计进程收尾 | 17 份 accepted 图片型样本复用缓存分批复跑，全部正常退出，无超时 | ✅ 已验证 | 2026-08-10 分批回归；争议与占位样本继续由来源门禁阻断 |
| 安装包 | **Konggu_0.4.0_x64-setup.exe**，SHA-256：`41c8c7d429642544b4e9b0bd272061e805c5372c767d0c58961722c0366f68ce`，大小 296,728,196 字节（≈283 MB），构建时间 2026-08-09 22:53，基于提交 `85f8461`；`konggu.exe` 13,774,848 字节，`konggu-worker.exe` 278,599,359 字节 | ✅ 已验证 | 2026-08-09 `npm.cmd run build` 构建，版本号 0.4.0，与源码一致 |
| 断网运行 | 本轮未验证 | 未验证 | 全新用户目录断网测试 |
| 前端 chunk 体积 | `index-BFUjs09z.js` 约 745 kB（gzip 219.69 kB），超过 vite 默认提示阈值 | 待评估 | 后续按需做代码分割优化 |

## 四、2026-08-09 PDF Inspector 0.4.0 事实

| 项目 | 当前事实 | 状态 | 证据说明 |
|---|---|---|---|
| 源码版本 | 0.4.0；功能提交 `e7ab4ef` | 已提交 | package.json / package-lock.json / Cargo.toml / Cargo.lock（konggu）/ tauri.conf.json / README 徽章 |
| Python binding | `pdf-inspector==0.2.6`，Windows x64 wheel 2,550,798 字节，MIT | 已验证 | PyPI wheel下载、导入与 GitHub `493fed4` 源码/许可证核验 |
| 集成边界 | sidecar 旁路诊断；不替换 PyMuPDF，不改变质量门禁和正式 OCR 路由 | 已验证 | `core/pdf_inspection.py`、工作流与复核页代码 |
| 资源限制 | 单文件 50 MB、100 页；失败回退 PyMuPDF | 已验证 | 单元测试 |
| Python 全量测试 | 97 passed, 2 warnings（最终复跑 10.96s） | 已验证 | 2026-08-09 `.venv\Scripts\python.exe -m pytest -q --basetemp <项目内独立目录>` |
| 前端构建 | 通过；Vite 8.1.3，40 modules，487ms；主 JS 747.34 kB（gzip 220.58 kB） | 已验证 | 2026-08-09 `npm.cmd run build:frontend` |
| 桌面层检查 | konggu v0.4.0，17.93s | 已验证 | 2026-08-09 `cargo check --manifest-path src-tauri\Cargo.toml --locked` |
| sidecar 冻结 | `konggu-worker.exe` 278,139,040 字节，SHA-256 `9D1E28E4BF77F4FCC1539A4B76766D7F4E40EF56BC219872DF8B91D6937B141F`；包含 `pdf_inspector.pyd`、包元数据、SBOM 与 MIT LICENSE，不含 `direct_url.json` | 已验证 | 2026-08-09 PyInstaller 构建、archive viewer 与冻结 `pdf.inspect` 冒烟 |
| 真实样本页级对账 | 70 份、109 页全部完成；53 文本型、16 图片型、1 扫描型；PDF Inspector 建议 OCR 26 页，与 PyMuPDF 的 26 个无文本层页面完全一致 | 已验证 | 2026-08-09 仓库外正式样本库只读聚合检查 |
| 真实课表准确率效果 | 未建立课程块人工真值，不能声明提升准确率或减少 OCR 耗时 | 未验证 | 需人工核对课程块、周次、星期和节次；逐页 OCR 尚未启用 |

## 五、2026-08-10 质量回归与隐私基线事实

| 项目 | 当前事实 | 状态 | 证据说明 |
|---|---|---|---|
| 源码版本 | 0.4.1；质量基线提交 `52c2c6a` | 已提交 | package.json / package-lock.json / Cargo.toml / Cargo.lock（konggu）/ tauri.conf.json / README 徽章 |
| 脱敏样本索引 | 35 组 accepted 配对、2 份仅单方、3 份阻断和 3 份已移除记录均使用合成成员标识；真实 PDF 仍在仓库外 | 已验证 | `config/reference_library_samples.json`、`tests/回归样本登记表.md` |
| 人工真值模板 | 35 组正常样本与 3 组阻断样本已建立空槽模板；`pending` 标注会被评估器拒绝，不构成准确率证据 | 已验证 | `docs/baselines/ground-truth-2026-08-10.json`、`tests/test_regression_quality_gates.py` |
| 质量门禁回归 | accepted / needs_review / blocked、导出门禁、人工修正签名绑定与失效、真值评估均有代表性测试 | 已验证 | 2026-08-10 Python 全量测试 121 passed、2 warnings（29.88s） |
| 前端构建 | Vite 8.1.3 构建通过，40 modules，352ms；主 JS 747.34 kB（gzip 220.58 kB），保留 chunk 体积提示 | 已验证 | 2026-08-10 `npm.cmd run build:frontend` |
| 桌面层检查 | konggu v0.4.1，18.60s | 已验证 | 2026-08-10 `cargo check --manifest-path src-tauri\\Cargo.toml --locked` |
| 隐私检查 | 本次改动已移除已知真实姓名和本机用户/样本库绝对路径；仓库仅保留合成标识与必要哈希 | 已验证 | 2026-08-10 `rg` 扫描与改动审查 |
| 0.4.1 安装包 | 未构建；现有已核验安装包仍为 0.4.0 | 未验证 | 本次仅提交源码、测试与文档，不建立标签或发布 |

## 六、2026-08-11 阿谷虚拟形象与 0.4.2 事实

| 项目 | 当前事实 | 状态 | 证据说明 |
|---|---|---|---|
| 源码版本 | 0.4.2；功能提交 `84bebd7` | 已提交、已验证 | package.json / package-lock.json / Cargo.toml / Cargo.lock（konggu）/ tauri.conf.json / README 徽章 |
| 角色身份 | 空谷虚拟形象“阿谷”；双叶冠、透明青心种仁、圆润谷粒身体 | 已确认 | `docs/品牌/阿谷角色与视觉使用规范.md`、D-013 |
| 品牌资产 | 四张确认源图及头像、应用图标、启动场景、宣传卡和 WebP 衍生图已登记尺寸与 SHA-256 | 已验证 | `assets/brand/agu/manifest.json`、`tools/build_agu_assets.py` 整体重建 |
| 完成立绘 | 1254×1254 RGBA；四角透明；SHA-256 `3ACB37F710034DD7A2FB31D10273E58294748D1FBF0F4C361F93B7D3CB15537C` | 已验证 | 本地透明度与哈希检查 |
| Python 全量测试 | 121 passed、2 warnings（31.28s） | 已验证 | 2026-08-11 提交前复跑：`.venv\Scripts\python.exe -m pytest -q --basetemp tmp\pytest-publish-042` |
| 前端构建 | Vite 8.1.3 构建通过，43 modules，635ms；主 JS 748.74 kB（gzip 221.14 kB） | 已验证 | 2026-08-11 提交前复跑：`npm.cmd run build:frontend`；保留 chunk 体积提示 |
| 桌面层检查 | konggu v0.4.2，3.79s | 已验证 | 2026-08-11 提交前复跑：`cargo check --manifest-path src-tauri\Cargo.toml --locked` |
| 界面视觉检查 | 默认窗口与 1080×820 最小窗口下欢迎页无裁切或遮挡；浏览器预览显示友好桌面端提示 | 已验证 | 2026-08-11 本地浏览器 DOM 与截图检查 |
| 0.4.2 安装包 | `Konggu_0.4.2_x64-setup.exe`；316,286,074 字节（301.63 MiB）；SHA-256 `5C619A59C5C774DF1681EC99711E2636F3FFDA97DB60A37E2CA364C15F05BD2F`；未代码签名 | 已构建、已静态验证 | 2026-08-11 基于提交 `fcc05f8` 执行 `npm.cmd run build`；应用文件版本与产品版本均为 0.4.2；`tools/verify_release_bundle.py --require-installer` 全项通过；安装、真实桌面启动、系统图标、断网、全新用户目录和卸载未验证 |

## 七、2026-08-11 用户语言与 0.5.0 工作区事实

| 项目 | 当前事实 | 状态 | 证据说明 |
|---|---|---|---|
| 源码版本 | 0.5.0；当前提交仍为 `fcc05f8` | 在研、未提交 | package.json / package-lock.json / Cargo.toml / Cargo.lock（konggu）/ tauri.conf.json / README 徽章一致 |
| 用户主流程 | 导入课表—生成空课表—待处理—原文核对—导出空课表；主界面隐藏质量门禁、解析签名、任务 ID、问题代码和检查引擎元数据；顶部使用“青心如禾，向阳而生”，欢迎区直接说明汇总课表与查找共同空闲 | 已实现、代码级已检查 | `src/main.ts`、`src/reviewView.ts`、`tests/test_frontend_user_language.py`、D-015；2026-08-11 文案修订后相关测试、TypeScript 和 Vite 通过，浏览器当前画面因本地 URL 安全策略未重新截屏 |
| 阿谷状态动效 | 独立 `aguMotion` 组件提供欢迎、处理中和完成有限姿态动画，并支持减少动态效果；3 项动效契约回归通过 | 在研、已完成代码级验证 | `src/aguMotion.ts`、`src/styles.css`、`tests/test_agu_motion_contract.py`、D-014；真实桌面逐态检查未完成 |
| 流畅界面生命周期 | 应用外壳只挂载一次；进度事件按动画帧合并；六个标签面板稳定保留；PDF 复核控制器提供 update / destroy / getViewState；日历仅在课程或设置变化时重建 | 已实现、已检查 | `src/main.ts`、`src/reviewView.ts`、`src/uiMotion.ts`、`tests/test_frontend_fluid_ui_contract.py`、D-016 |
| Python 全量测试 | 132 passed、2 warnings（11.05s） | 已验证 | 2026-08-11 当前流畅 UI 工作区：`.venv\Scripts\python.exe -m pytest -q --basetemp tmp\pytest-ui-fluid-full` |
| TypeScript 检查 | `tsc --noEmit` 通过；前端目标与 lib 统一为 ES2021 | 已验证 | 2026-08-11 `node_modules\.bin\tsc.cmd --noEmit` |
| 前端构建 | Vite 8.1.3，46 modules，396ms；主 JS 759.50 kB（gzip 224.50 kB） | 已验证 | 2026-08-11 `npm.cmd run build:frontend`；保留 chunk 体积提示 |
| 桌面层检查 | konggu v0.5.0，3.14s | 已验证 | 2026-08-11 `cargo check --manifest-path src-tauri\Cargo.toml --locked` |
| 浏览器界面检查 | 1280×720 和 1080×820 下无横向溢出；稳定外壳 1 个、标签面板 6 个；鼠标和方向键切换正确，标签指示条随窗口尺寸更新，控制台无 warning / error | 已验证 | 2026-08-11 应用内浏览器 DOM、边界、截图与交互检查；浏览器无法连接 Tauri 本地识别功能 |
| 阿谷动画视觉检查 | 浏览器欢迎状态已看到静态与动作姿态且无控制台错误；working / complete、Windows 减少动态效果和真实 Tauri 裁切仍未完成逐态验收 | 部分验证 | 需要在真实 Tauri 中逐态检查 welcome / working / complete；不得用浏览器欢迎页代替全部状态 |
| 0.5.0 安装包 | 既有 `Konggu_0.5.0_x64-setup.exe`（316,384,872 字节；SHA-256 `74214057FD29DA8151C190C1292D9A147B969809351FB14D4E39C7852F8849D2`）早于本轮流畅 UI 改造 | 历史构建、落后当前源码 | 该包曾通过静态检查，但不包含稳定外壳、局部渲染和新视觉层；当前源码对应安装包未构建，不能将旧包作为本轮候选包 |

## 八、2026-08-12 0.5.0 紧凑界面、质量修正与候选包验收

| 项目 | 当前事实 | 状态 | 证据说明 |
|---|---|---|---|
| 紧凑高度模式 | 高度不超过约 760px 时压缩工作台、侧栏和顶部状态区间距，隐藏非关键最近活动；侧栏底部隐私入口、核心按钮和内容卡片仍在视口内；外层不新增不可达滚动 | 已验证 | `src/styles.css`、`tests/test_frontend_fluid_ui_contract.py`；浏览器 1080×720 与 1280×820 边界检查 |
| 界面设计规范 | 固定品牌、颜色、字体层级、间距、圆角、状态、阿谷、动效和无障碍规则，明确禁止紫色渐变模板感、发光悬停、无业务价值 3D/视差和在线运行时素材 | 已实现 | `docs/界面设计规范.md`、README 入口 |
| 输入预检修正 | 预检错误继承为 `INPUT_PREFLIGHT_FAILED` 阻断 issue；仅有单边中方或英方课表时为 `MEMBER_SCHEDULE_INCOMPLETE`，禁止正式导出 | 已验证 | `app/services/desktop_workflow.py`、`core/quality.py`；回归测试与真实重复/缺配对样本 |
| Python 全量测试 | 137 passed、2 warnings | 已验证 | 2026-08-12 `.venv\Scripts\python.exe -m pytest -q --basetemp <仓库外隔离目录>` |
| 前端与桌面检查 | TypeScript、Vite 前端构建、`cargo check --manifest-path src-tauri\Cargo.toml --locked` 通过 | 已验证 | 2026-08-12 当前工作区 |
| 发布包 | `Konggu_0.5.0_x64-setup.exe`，316,372,567 字节，SHA-256 `2089FF242365F488D967164E0546C2555A01B6698309113D4D99C3D83BB15608`；离线清单 28 项完整性与精确安装包静态检查通过 | 已构建、已验证 | 2026-08-11/12 `scripts/build-release.cmd`、`tools/verify_release_bundle.py --require-installer` |
| 安装版离线 worker | 在临时出站阻断规则下完成资源修复；真实文本层中英配对 `accepted` 并导出；零文本层中英配对本地 OCR `accepted` 并导出 | 已验证 | 安装版 `konggu-worker.exe`，专用隔离 appdata；不构成课程块准确率证据 |
| 安装版桌面启动 | 窗口标题“空谷 Konggu”，进程响应正常，子进程包含 `konggu-worker.exe --serve`；截图保存在仓库外临时验收目录 | 已验证 | 2026-08-12 Windows 实机启动 |
| 卸载—重装 | 前一轮循环观察到卸载项和程序目录移除后重新安装；本轮再次触发卸载器时卡在系统权限确认，当前电脑保留最终候选包 | 部分验证 | UAC 需人工完成；不强删程序目录 |
| 现有用户数据目录 | 本轮验收前后 `%LOCALAPPDATA%\Konggu` 摘要由 39 文件/44,416,417 字节变为 55 文件/54,315,459 字节；本轮后续未再写入、删除或回滚该目录 | 待调查 | 需查明资源迁移来源；不得表述为“未变化” |

## 九、2026-08-12 0.5.1 原文核对与导出显示修正

| 项目 | 当前事实 | 状态 | 证据说明 |
|---|---|---|---|
| 原文核对滚轮 | PDF 预览支持普通滚轮浏览，Ctrl/⌘+滚轮缩放；右侧核对面板和 PDF 视图区均保留独立滚动 | 已验证 | `src/reviewView.ts`、`src/styles.css`、`tests/test_frontend_user_language.py` |
| 文件切换辨识 | 当前文件选择器显示文件名、课表方向和文件序号，深色选项具备高对比度 | 已实现、已检查 | `src/reviewView.ts`、`src/styles.css`、前端契约测试 |
| 品牌文案 | 主界面删除“青禾计划 · 空谷”眉题；“青心如禾，向阳而生”使用绿色、加粗华文中宋优先字体 | 已实现、已检查 | `src/main.ts`、`src/styles.css`、`tests/test_frontend_user_language.py` |
| Excel 姓名显示 | 导出边界将组合成员标识转换为姓名，周表、空课明细、课程占用、成员课程和文件记录不再输出部门/职务 | 已验证 | `core/schedule_core.py`、`tests/test_export_excel_service.py` |
| 相关回归 | 15 个相关 Python 测试通过；Vite 前端构建通过；仍保留既有 chunk 体积提示 | 已验证 | 2026-08-12 当前工作区 |
| 0.5.1 安装包 | `Konggu_0.5.1_x64-setup.exe`，316,381,232 字节，SHA-256 `2FCBF6E4AF4AA58F0DD103938C99BF4A04D14AF267E92B3431FFA57D7878573B`；离线清单 28 项、sidecar、PDF worker 和安装包版本检查通过 | 已构建、已静态验证 | 2026-08-13 `scripts/build-release-local-tools.ps1`、`tools/verify_release_bundle.py --require-installer`；安装实机、断网和卸载尚未验证 |

## 十、2026-08-13 0.5.1 安装包构建

| 项目 | 当前事实 | 状态 | 证据说明 |
|---|---|---|---|
| 发布构建 | 使用 `scripts/build-release-local-tools.ps1` 调用 `npm.cmd run build`，重新打包前端、Python worker、Tauri 主程序和 NSIS 安装器 | 已验证 | Windows 11 / Visual Studio 2022 Build Tools / Python 3.12.4 / PyInstaller 6.21.0 / Vite 8.1.3 / Cargo release build 日志 |
| 安装包静态门禁 | 依赖、权限、PDF worker、sidecar、离线资源清单 28 项、truth manifest 和 NSIS 文件名均通过 | 已验证 | `tools/verify_release_bundle.py --require-installer` |
| 安装与断网运行 | 本轮尚未在真实另一台电脑安装 0.5.1 包；已用新冻结 worker 在隔离全新用户目录完成 `resources.repair`，复制 28 项资源并返回 `ready: true` | 部分验证 | 2026-08-13 直接运行 `src-tauri\\binaries\\konggu-worker-x86_64-pc-windows-msvc.exe --serve`；真实安装、断网、导入、导出和卸载实机验证仍待补 |

## 十一、2026-08-13 0.5.1 首次安装与停止处理修复

| 项目 | 当前事实 | 状态 | 证据说明 |
|---|---|---|---|
| 首次安装修复识别 | 强制 sidecar 标准输入输出使用 UTF-8，并让 Windows ANSI 标准流以替换策略输出，避免 PaddleOCR 中文异常导致 `invalid utf-8 sequence`；新冻结 worker 的隔离资源修复返回 `ok: true`、28 项复制、OCR `ready: true` | 已验证 | `app/sidecar.py`、`src/sidecarClient.ts`、`tests/test_sidecar.py`、`tests/test_frontend_sidecar_encoding.py`；2026-08-13 隔离全新用户目录冒烟 |
| 停止处理 | 前端以任务 ID 固定“取消中”状态，忽略取消请求后的迟到进度；超过 10 秒仍无终态时重启 sidecar 并恢复可操作界面；后端在输入加入和 PDF 预检后增加取消检查 | 已验证 | `src/main.ts`、`app/services/desktop_workflow.py`、`tests/test_frontend_fluid_ui_contract.py`、`tests/test_desktop_workflow.py` |
| Python 全量测试 | 144 passed、2 warnings（17.53s） | 已验证 | 2026-08-13 `.venv\\Scripts\\python.exe -m pytest -q --basetemp <项目内隔离目录>` |
| TypeScript / 前端 / 桌面检查 | TypeScript `tsc --noEmit`、Vite 前端构建和锁定依赖 Cargo 检查均通过；Vite 保留主 chunk 体积提示 | 已验证 | 2026-08-13 `node_modules\\.bin\\tsc.cmd --noEmit`、`npm.cmd run build:frontend`、`cargo check --manifest-path src-tauri\\Cargo.toml --locked` |
| 当前 0.5.1 安装包 | `Konggu_0.5.1_x64-setup.exe`，316,395,860 字节，SHA-256 `5B5F67A7C5B6940664BAFF289FAEB49386485D2BE7D9B88D6AB60585975DF25C`；worker 287,968,177 字节 | 已构建、已静态验证 | 2026-08-13 `npm.cmd run build` 后，`tools/verify_release_bundle.py --require-installer` 全项通过；仍未在用户所说的另一台电脑完成真实安装验收 |

## 十二、2026-08-14 任务问题持久化隔离修复

| 项目 | 当前事实 | 状态 | 证据说明 |
|---|---|---|---|
| `issues` 主键 | 改为任务范围内的 `(job_id, issue_id)`；同一问题可出现在不同提交任务中，同一任务内仍保持唯一 | 已验证 | `app/services/state_store.py` schema 2；跨任务复用 ID 回归通过 |
| 旧数据库迁移 | schema 1 → 2 自动重建 `issues` 表，保留问题、确认状态和确认时间 | 已验证 | 旧 schema 构造库迁移测试通过 |
| 结果保存原子性 | 任务完成状态、结果快照和问题记录在同一事务中保存；问题写入失败时回滚任务完成状态 | 已验证 | 重复问题 ID 事务回滚测试通过 |
| Python 全量测试 | `156 passed, 2 warnings` | 已验证 | 2026-08-14 `.venv\\Scripts\\python.exe -m pytest -q --basetemp tmp\\pytest-security-final` |
| 发布状态 | 当前源码工作区为 0.5.2；未构建新安装包、未提交、未推送、未打标签 | 在研 | 本轮完成源码安全修复、前端构建、TypeScript 和 Cargo locked check |

## 十三、维护规则

- 所有版本、数字和指标必须注明日期、提交和验证环境。
- 新事实写入前必须有可核验证据。
- 历史事实不得直接覆盖当前记录。
- 状态变化后同步更新 `MEMORY.md`。

## 十四、2026-08-14 0.5.2 安全与隐私加固

| 项目 | 当前事实 | 状态 | 证据说明 |
|---|---|---|---|
| 安全修复范围 | Excel 公式注入、路径与结果留存、Tauri CSP/权限、PDF 资源门禁、manifest containment、错误脱敏、Python 依赖版本锁定已完成代码级修复 | 已验证 | `security_best_practices_report.md`；对应专项回归和静态检查通过 |
| 本地数据生命周期 | 任务/修正默认保留 30 天；接受任务清除数据库源路径；增加 `data.clear` 清理结果、修正、审计、解析/OCR缓存和 WAL；当前会话待复核仍可读取原文 | 已验证 | `app/services/state_store.py`、`app/offline_resources.py`、`app/sidecar.py`、前端清理入口；154 个 Python 测试通过 |
| 版本 | 源码版本已升至 0.5.2；未构建新安装包、未提交、未推送、未打标签 | 在研 | `package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、README 徽章；2026-08-14 |
| 验证 | Python 154 passed、2 warnings；Vite 构建、compileall、密钥扫描和 Tauri 权限静态检查通过 | 已验证 | 2026-08-14，项目内 `tmp\pytest-security`；真实安装/断网/卸载/多账户 ACL/模糊测试仍待执行 |

## 十五、2026-08-14 0.5.2 候选包构建与发布校验收口

| 项目 | 当前事实 | 状态 | 证据说明 |
|---|---|---|---|
| 发布门禁 | `tools/verify_release_bundle.py --require-installer` 已移除对旧 `tauri-plugin-persisted-scope` 的过期检查，当前 Cargo 插件集合与桌面实现一致 | 已验证 | 2026-08-14 当前工作区；发布包静态校验全项通过 |
| Python / 前端 / 桌面检查 | Python `156 passed, 2 warnings`；TypeScript、Vite 前端构建和 `cargo check --manifest-path src-tauri\Cargo.toml --locked` 通过 | 已验证 | 2026-08-14 当前工作区；Vite 保留主 chunk 体积提示 |
| 0.5.2 安装包 | `Konggu_0.5.2_x64-setup.exe`，316,389,652 字节，SHA-256 `2444E9FDC97CF580132AF2D414FA10CF36D42D7FF23427A0BF30AA1A37CC704F`；`konggu-worker.exe` 287,979,606 字节，SHA-256 `85C1DA499E96A7D3CF8268C8D99D52E9F1B480D06AEE09FD37CCE10A3A1770DF` | 已构建、已静态验证 | 2026-08-14 `npm.cmd run build` 与 `tools/verify_release_bundle.py --require-installer` |
| 全新用户目录与启动冒烟 | 当前 worker 在全新目录复制 28 项离线资源并返回 `ready: true`，响应不含绝对路径；当前 release 可执行文件能启动 Tauri 窗口并拉起 worker | 已验证 | 2026-08-14 当前 release 直接启动和隔离临时目录冒烟 |
| 尚未完成的发布验收 | 安装包未签名；尚未在另一台电脑完成真实安装、断网、WebView2、卸载、多账户 ACL、导入/预览/复核/导出和用户数据库迁移验收 | 未验证 | 这些检查不能由源码、静态包检查或当前电脑启动冒烟替代 |
